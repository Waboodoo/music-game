const Clef = {
    TREBLE: 'treble',
    BASS: 'bass',
};

const NoteModifier = {
    FLAT: 'flat',
    SHARP: 'sharp',
};

const GameState = {
    RUNNING: 'running',
    PAUSED: 'paused',
    COMPLETE: 'complete',
};